---
interface Props {
  text?: string;
}

const { text = 'Now teaching at GolfCube Social, Springfield, NJ  |  Private indoor lessons on GOLFZON simulators' } = Astro.props;
---

<div class="gold-bar" set:html={text} />
